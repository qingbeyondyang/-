# Modular Split Versions

This folder contains two refactored versions of the original `app.py`:

- `zh/`: Chinese UI + Chinese comments/messages.
- `en/`: English UI + English API responses (shares core runtime with `zh`).
